// ============================================================
// Configuration Template — Copy to config.js and fill in
// ============================================================

const CONFIG = {
    // Supabase — Get from https://supabase.com/dashboard
    supabaseUrl: '',
    supabaseKey: '',

    // Streak CRM — Get from Streak → Settings → Integrations → API
    streakApiKey: '',

    // Gmail OAuth — Get from Google Cloud Console
    // See setup instructions in the app (Integrations → Gmail → Connect)
    googleClientId: '',

    // Gmail API scopes (don't change)
    gmailScopes: 'https://www.googleapis.com/auth/gmail.readonly https://www.googleapis.com/auth/gmail.labels',
};

export default CONFIG;
